spring:
  application:
    name: application
  profiles:
    active: dev
    include: secret
  config:
    import: optional:configserver:http://localhost:8888 # config server url
  cloud:
    config:
      label: main

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*" # test only

encrypt:
  key: ${ENCRYPT_KEY}